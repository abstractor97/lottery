
namespace GameReqs {

    // export function checkChance() {
    //     return new Promise(async resolve => {
    //         let param = `openId=${P.getGameVOProxy().openId}`;
    //         let signstr = `${P.getGameVOProxy().openId}|SHUHEKEJI`;
    //         let sign = "&sign=" + Tools.madeMd5(signstr);
    //         let url = GameConfig.checkChanceUrl + param + sign;
    //         let data = await Http.getInstance().httpReq(egret.HttpMethod.GET, url);
    //         resolve(data);
    //     });
    // }


}